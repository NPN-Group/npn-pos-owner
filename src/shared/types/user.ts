export type User = {
    id: string;
    email: string;
    phone: string;
    firstName: string;
    lastName: string;
    img: string;
    role: string;
    createdAt: string;
    updatedAt: string;
}
